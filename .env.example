# StremThru (ElfHosted) – required for playback. Create a wrapped addon (Wrap page, Store = StremThru),
# then set the base URL without /manifest.json. Playback stays on your site; Real-Debrid sees one IP.
STREMTHRU_STREAM_BASE_URL=https://YOUR-USER-stremthru.elfhosted.com/stremio/wrap/XXXXX
# STREMTHRU_TOKEN=optional_token_if_your_wrap_requires_it

# TMDB API key used by Vercel /api/tmdb proxy
# Get one at https://www.themoviedb.org/settings/api
TMDB_API_KEY=your_tmdb_key_here

# Optional: port (default 3000) when running server.py locally
# PORT=3000
